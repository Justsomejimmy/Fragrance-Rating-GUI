export struct UserRating {
    user_id: int,
    user_name: string,
    rating: float,
}

export struct AppUser {
    id: int,
    name: string,
}

export struct NoteChoice {
    name: string,
    selected: bool,
    matches: bool,
}

export struct FragranceData {
    id: int,
    brand: string,
    name: string,
    concentration: string,
    projection: string,
    longevity: string,
    price: string,
    purchase_date: string,
    rating: float,
    notes: string,
    seasons: string,
    image: image,
    my_notes: string,
    partner_notes: string,
    image_path: string,
    image_offset_x: float,
    image_offset_y: float,
    image_scale: float,
    season_spring: bool,
    season_summer: bool,
    season_fall: bool,
    season_winter: bool,
    category: string,
    ratings: [UserRating],
    is_wishlist: bool,
    note_choices: [NoteChoice],
}

export struct FragranceInput {
    id: int,
    brand: string,
    name: string,
    concentration: string,
    projection: string,
    longevity: string,
    price: string,
    purchase_date: string,
    season_spring: bool,
    season_summer: bool,
    season_fall: bool,
    season_winter: bool,
    category: string,
    my_notes: string,
    partner_notes: string,
    image_path: string,
    image_offset_x: float,
    image_offset_y: float,
    image_scale: float,
    ratings: [UserRating],
    send_to_wishlist: bool,
    note_choices: [NoteChoice],
}

export struct ImageSelection {
    path: string,
    picture: image,
}

export struct FragranceRow {
    fragrances: [FragranceData],
}

export struct DashboardData {
    total: int,
    average_rating: float,
    highest_rated: FragranceData,
    lowest_rated: FragranceData,
    most_common_season: string,
    season_top: [FragranceData],
    recently_added: [FragranceData],
    top_cologne: [FragranceData],
    top_perfume: [FragranceData],
}

export struct RankingItem {
    id: int,
    brand: string,
    name: string,
    image: image,
    image_offset_x: float,
    image_offset_y: float,
    image_scale: float,
    display_rating: float,
}

export struct ImportResult {
    success: bool,
    error: string,
    brand: string,
    name: string,
    notes: string,
    price: string,
    image_path: string,
}

export struct ValidationErrors {
    brand: string,
    name: string,
    concentration: string,
    projection: string,
    longevity: string,
    price: string,
    purchase_date: string,
    my_notes: string,
    partner_notes: string,
    notes: string,
    has_errors: bool,
}

export enum Page {
    Collection,
    Details,
    Edit,
    Add,
    Dashboard,
    Settings,
    Rankings,
    Wishlist,
}